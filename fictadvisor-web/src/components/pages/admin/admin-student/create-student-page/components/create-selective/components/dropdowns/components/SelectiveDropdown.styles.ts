import { SxProps, Theme } from '@mui/material/styles';

export const selectedSelective: SxProps<Theme> = {
  width: '100%',
  maxWidth: '372px',
  display: 'flex',
  alignItems: 'center',
  gap: '10px',
};
