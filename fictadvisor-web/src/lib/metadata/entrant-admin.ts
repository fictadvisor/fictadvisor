import { Metadata } from 'next';

const title = 'Менеджент вступника | FICE Advisor';

const entrantAdminMetadata: Metadata = {
  title,
  openGraph: {
    title,
  },
};
export default entrantAdminMetadata;
