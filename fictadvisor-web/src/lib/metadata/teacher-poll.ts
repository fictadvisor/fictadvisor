import { Metadata } from 'next';

const title = 'Опитування викладачів | FICE Advisor';

const teacherPollMetadata: Metadata = {
  title,
  openGraph: {
    title,
  },
};
export default teacherPollMetadata;
