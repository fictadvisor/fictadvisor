import { SxProps, Theme } from '@mui/material/styles';

export const wrapper: SxProps<Theme> = {
  display: 'flex',
  flexDirection: 'column',
  gap: '10px',
  width: '100vw',
  minHeight: '100vh',
  padding: '10px',
};
