# Copy to ~/deployment/env/postgres-exporter-dev.env
#
# Connection string for the DEV postgres-exporter -> postgres-db-dev.
# The password must match POSTGRES_PASSWORD in ~/deployment/env/postgres-dev.env.
DATA_SOURCE_NAME=postgresql://postgres:CHANGE_ME@postgres-db-dev:5432/postgres?sslmode=disable
